{
  "Version": "2012-10-17",
  "Statement": [
    {
      "Sid": "",
      "Effect": "Allow",
      "Action": [
        "lambda:UpdateFunctionCode"
      ],
      "Resource": [
        "arn:aws:lambda:${region}:${account_id}:function:${site_name}-trigger-codepipeline",
        "arn:aws:lambda:${region}:${account_id}:function:${site_name}-staging-trigger-codepipeline"
      ]
    }
  ]
}
